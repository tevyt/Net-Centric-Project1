Net-Centric-Project1
====================

This is a project that uses a python socket library to create a tcp server which upon request returns an html file from the servers file system to the client
